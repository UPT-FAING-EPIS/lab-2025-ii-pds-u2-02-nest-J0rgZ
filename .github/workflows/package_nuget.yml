name: deprecated - do not use (migrated to package_npm.yml)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Este workflow fue reemplazado por .github/workflows/package_npm.yml"